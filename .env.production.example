# Production Environment Configuration Template
# Rename to .env in production

# --- Application Settings ---
DEBUG=False
ENVIRONMENT=production
# Domain of your application (e.g., https://myapp.com)
APP_DOMAIN=https://your-domain.com

# --- Security ---
# Generate a secure key using: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=change-this-to-a-secure-random-string

# --- Database (PostgreSQL) ---
# If using Docker Compose: postgresql://user:password@db:5432/dbname
# If using Managed DB: postgresql://user:password@host:port/dbname
DATABASE_URL=postgresql://postgres:postgres@db:5432/clipboard_sync
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=clipboard_sync

# --- Cache (Redis) ---
# If using Docker Compose: redis://redis:6379/0
REDIS_URL=redis://redis:6379/0

# --- Google OAuth ---
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-production-client-id
GOOGLE_CLIENT_SECRET=your-production-client-secret
# Must properly match your production domain
GOOGLE_REDIRECT_URI=https://your-domain.com/api/v1/auth/google/callback

# --- Frontend specific (if building separately) ---
NEXT_PUBLIC_API_URL=https://your-domain.com/api/v1
NEXT_PUBLIC_WS_URL=wss://your-domain.com/api/v1/ws
